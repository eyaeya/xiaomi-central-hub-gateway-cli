{
  "name": "xgg-public-release",
  "version": "0.1.0",
  "private": true,
  "description": "Public release workspace for xgg, the Xiaomi Gateway Geek Edition CLI",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm lint && pnpm build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "pack:release": "rm -rf release-artifacts && pnpm build && pnpm -C packages/core pack --pack-destination ../../release-artifacts && pnpm -C packages/cli pack --pack-destination ../../release-artifacts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
